{
  "name": "hozyan-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:seed": "npm --prefix server run seed",
    "render-build": "npm --prefix server install && npm --prefix client install --include=dev && npm --prefix client run build:render",
    "render-start": "npm --prefix server start",
    "start:prod": "npm run render-build && npm run render-start",
    "seed": "npm --prefix server run seed",
    "render-seed": "npm --prefix server run seed"
  }
}
